      <nav class="navbar">
        <div class="container navbar-content">
          <div>
            <%= link_to  root_path, class: "navbar-brand" do %>
              <div><%= image_tag "exercices/logo.png", class: "nav-logo" %>
              <span class="text-gray">Electron Formation</span>
              <span class="badge badge-info"><%= current_user.role %></span>
              </div>
            <% end %>
          </div>
          <ul class="navbar-menu">
           
            
            <% if current_user.admin? %>
              <li><%= link_to "Utilisateurs", admin_users_path, class: "navbar-link" %></li>

            <% end %>
            
            <% if current_user.formateur?  %>
              
              
            <% end %>
            
            <li style="margin-left: auto;">
              <span style="color: var(--gray-600);"></span>
            </li>
            <li><%= link_to "#{ current_user.full_name} ➜]", destroy_user_session_path, data: { turbo_method: :delete }, class: "navbar-link bg-danger" %></li>
          </ul>
        </div>
      </nav>